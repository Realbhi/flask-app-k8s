apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc2-nfs
  namespace: nfs-nfs
spec:
  accessModes: 
  - ReadWriteMany
  storageClassName: nfs-sc 
  resources:
    requests:
      storage: 600Mi
