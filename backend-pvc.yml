apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-pvc
  namespace: trip-planner
spec:
  resources:
    requests:
      storage: 1.5Gi
  storageClassName: dummybackend
  accessModes:
    - ReadWriteOnce
